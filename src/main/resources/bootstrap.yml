server:
  port: 8090
spring:
  security:
    user:
      name: pr0j3ct
      password: pr0j3ct
  cloud:
    config:
      server:
        git:
          uri: https://github.com/uCrover/config-data.git
          default-label: "main"
          #searchPaths: /config-data
  application:
    name: config-service